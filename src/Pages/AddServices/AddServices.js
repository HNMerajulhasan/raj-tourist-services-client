import React from 'react';
import useTitle from '../../hooks/useTitle';

const AddServices = () => {
   useTitle('AddServices'); 
    return (
        <div>
            <h2>Here we will add more services</h2>
        </div>
    );
};

export default AddServices;